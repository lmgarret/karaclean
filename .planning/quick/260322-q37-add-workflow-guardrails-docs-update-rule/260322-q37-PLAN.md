---
phase: quick
plan: 260322-q37
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [q37]
must_haves:
  truths:
    - "CLAUDE.md exists at project root with workflow guardrails"
    - "golangci-lint passes on current codebase"
    - "go test passes on current codebase"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project conventions and pre-commit workflow rules"
  key_links: []
---

<objective>
Create CLAUDE.md with workflow guardrails so Claude always updates docs alongside features, runs lint, and runs tests before committing.

Purpose: Prevent documentation drift and lint/test regressions by encoding rules in CLAUDE.md.
Output: CLAUDE.md at project root; verified clean lint and test runs.
</objective>

<execution_context>
@/var/home/lm/git/karaclean/.claude/get-shit-done/workflows/execute-plan.md
@/var/home/lm/git/karaclean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.golangci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify lint and tests pass on current codebase</name>
  <files></files>
  <action>
Run `~/go/bin/golangci-lint run ./...` from project root to confirm lint passes.
Run `go test -race ./...` to confirm tests pass.
If either fails, fix the issues before proceeding to Task 2.
  </action>
  <verify>
    <automated>~/go/bin/golangci-lint run ./... && go test -race ./...</automated>
  </verify>
  <done>Both golangci-lint and go test exit 0 with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create CLAUDE.md with workflow guardrails</name>
  <files>CLAUDE.md</files>
  <action>
Create CLAUDE.md at project root with the following sections:

**Project Overview** (brief):
- Go project: Docker sidecar for Karakeep bookmark cleanup
- Declarative YAML rules, cron-scheduled, safety-first design

**Pre-Commit Checklist** (the core guardrails):
1. Documentation rule: When features are added or modified, documentation (README.md, example config in docs/karaclean.example.yaml) MUST be updated in the same commit or PR. Do not defer docs to a follow-up.
2. Lint rule: Run `~/go/bin/golangci-lint run ./...` before committing. Fix all lint errors. Config is in `.golangci.yml` (v2 format, standard linters plus gocyclo, godot, misspell, noctx).
3. Test rule: Run `go test -race ./...` before committing. Fix all test failures.

**Project Conventions**:
- Config parsing uses `go.yaml.in/yaml/v3` (maintained fork, NOT gopkg.in)
- Pointer types for optional config fields (nil vs zero-value distinction)
- `KnownFields(true)` for strict YAML parsing -- no custom UnmarshalYAML unless required for compatibility
- Case-sensitive matching throughout
- Validate() returns `[]ValidationError` for caller flexibility

**Tech Stack**:
- Go (module: github.com/lemonase/karaclean)
- golangci-lint v2.11 (binary at ~/go/bin/golangci-lint)
- Docker multi-stage build with scratch final image
- CI: GitHub Actions (golangci-lint-action@v9, go test -race)

Keep the file concise and actionable -- it is a prompt for Claude, not human documentation.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && grep -q "golangci-lint" CLAUDE.md && grep -q "README.md" CLAUDE.md && grep -q "go test" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md exists with all three guardrail rules (docs update, lint, test) and project context.</done>
</task>

</tasks>

<verification>
- `cat CLAUDE.md` shows all three pre-commit rules
- `~/go/bin/golangci-lint run ./...` exits 0
- `go test -race ./...` exits 0
</verification>

<success_criteria>
CLAUDE.md exists at project root with documentation-update, lint, and test guardrails. Current codebase passes both lint and tests.
</success_criteria>

<output>
After completion, create `.planning/quick/260322-q37-add-workflow-guardrails-docs-update-rule/260322-q37-SUMMARY.md`
</output>
