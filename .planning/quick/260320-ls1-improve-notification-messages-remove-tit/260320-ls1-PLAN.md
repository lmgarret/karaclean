---
phase: quick
plan: 260320-ls1
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/engine/notify.go
  - internal/engine/notify_test.go
  - internal/engine/run_test.go
autonomous: true
must_haves:
  truths:
    - "Notification message body does NOT contain the title line ([karaclean] rule-name)"
    - "Notification message body starts with Summary: prefix before the stats lines"
    - "Title remains unchanged (FormatNotificationTitle is untouched)"
  artifacts:
    - path: "internal/engine/notify.go"
      provides: "FormatNotification without title duplication, with Summary prefix"
    - path: "internal/engine/notify_test.go"
      provides: "Updated test expectations for new message format"
    - path: "internal/engine/run_test.go"
      provides: "Updated integration test assertion"
  key_links:
    - from: "internal/engine/notify.go"
      to: "internal/engine/run.go"
      via: "FormatNotification called in dispatchNotifications"
      pattern: "FormatNotification"
---

<objective>
Improve notification message format: remove title duplication from message body and add "Summary:" prefix.

Purpose: Currently FormatNotification produces a body starting with `[karaclean] <rule-name>` which duplicates FormatNotificationTitle. The body should contain only the stats, prefixed with "Summary:".
Output: Updated FormatNotification output, updated tests.
</objective>

<execution_context>
@/var/home/lm/git/karaclean/.claude/get-shit-done/workflows/execute-plan.md
@/var/home/lm/git/karaclean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/engine/notify.go
@internal/engine/notify_test.go
@internal/engine/run_test.go

<interfaces>
From internal/engine/notify.go:
```go
func FormatNotification(rs *RuleSummary, dryRun bool) string
func FormatNotificationTitle(ruleName string, dryRun bool) string
```

Current FormatNotification output (example):
```
[karaclean] old-rss       <-- THIS LINE IS THE TITLE DUPLICATION, REMOVE IT
deleted: 12 (4.2 MB)
archived: 3
excepted: 1
```

Desired FormatNotification output:
```
Summary:
deleted: 12 (4.2 MB)
archived: 3
excepted: 1
```

FormatNotificationTitle is NOT changed -- it stays as `[karaclean] old-rss`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update FormatNotification and all tests</name>
  <files>internal/engine/notify.go, internal/engine/notify_test.go, internal/engine/run_test.go</files>
  <behavior>
    - FormatNotification returns body starting with "Summary:" (no rule name, no [karaclean] prefix)
    - Stats lines follow after "Summary:\n"
    - Dry-run: NO dry-run prefix in body (title handles it) -- body is always just "Summary:\n..."
    - FormatNotificationTitle is unchanged
    - Run test assertion checks for "Summary:" instead of "[karaclean]"
  </behavior>
  <action>
1. In `internal/engine/notify.go`, modify `FormatNotification` (line 66-92):
   - Remove the dryRun prefix block (lines 69-71) -- dry-run indication is only in the title now
   - Replace the `[karaclean] %s` line (line 72) with `fmt.Fprintf(&b, "Summary:")`
   - Keep all the stat lines exactly as they are (they already use `\n` prefix which works perfectly after "Summary:")

2. In `internal/engine/notify_test.go`, update ALL `want` strings in TestFormatNotification:
   - Replace `"[karaclean] old-rss\n..."` with `"Summary:\n..."`
   - Replace `"[DRY-RUN] [karaclean] old-rss\n..."` with `"Summary:\n..."` (dry-run no longer affects body)
   - Replace `"[karaclean] web-junk\n..."` with `"Summary:\n..."`
   - Replace `"[karaclean] failing-rule\n..."` with `"Summary:\n..."`
   - Replace `"[karaclean] clean-rule\n..."` with `"Summary:\n..."`
   - Replace `"[karaclean] delete-only\n..."` with `"Summary:\n..."`
   - Replace `"[karaclean] archive-only\n..."` with `"Summary:\n..."`
   - The "dry run prefix" test case: update name to reflect body no longer has dry-run prefix, update want to just "Summary:\n..." same as non-dry-run equivalent

3. In `internal/engine/run_test.go` line 335-336: Change assertion from `strings.Contains(mn.calls[0].message, "[karaclean]")` to `strings.Contains(mn.calls[0].message, "Summary:")` with matching error message.

4. Update the doc comment on FormatNotification to reflect the new format.
  </action>
  <verify>
    <automated>cd /var/home/lm/git/karaclean && go test ./internal/engine/ -run "TestFormatNotification|TestFormatNotificationTitle|TestHasActivity|TestResolveChannelURL|TestRun_Notification" -v</automated>
  </verify>
  <done>FormatNotification returns "Summary:\ndeleted: N..." without title duplication. All tests pass. FormatNotificationTitle unchanged.</done>
</task>

</tasks>

<verification>
cd /var/home/lm/git/karaclean && go test ./internal/engine/ -v
cd /var/home/lm/git/karaclean && go vet ./internal/engine/
</verification>

<success_criteria>
- FormatNotification output starts with "Summary:" not "[karaclean] rule-name"
- No dry-run prefix in message body (title handles it)
- FormatNotificationTitle unchanged
- All engine tests pass
- go vet clean
</success_criteria>

<output>
After completion, create `.planning/quick/260320-ls1-improve-notification-messages-remove-tit/260320-ls1-SUMMARY.md`
</output>
