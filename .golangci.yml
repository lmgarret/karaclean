version: "2"

linters:
  default: standard
  enable:
    - gocyclo
    - godot
    - misspell
    - noctx

  settings:
    gocyclo:
      min-complexity: 15
    godot:
      scope: toplevel
    errcheck:
      check-type-assertions: true
